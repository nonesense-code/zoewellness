<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Restricted Registration Access</title>
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
    />
  </head>
  <body>
    <div class="container mt-5">
      <h1 class="text-center">Restricted Registration Access</h1>

      <% if (error) { %>
      <div class="alert alert-danger"><%= error %></div>
      <% } %>

      <form action="/authorizedparty" method="POST">
        <div class="form-group">
          <label for="authEmail">Authorization Email</label>
          <input
            type="email"
            name="email"
            id="authEmail"
            class="form-control"
            placeholder="Enter authorization email"
            required
          />
        </div>

        <div class="form-group">
          <label for="authPassword">Authorization Password</label>
          <input
            type="password"
            name="password"
            id="authPassword"
            class="form-control"
            placeholder="Enter authorization password"
            required
          />
        </div>

        <button type="submit" class="btn btn-primary">
          Authorize & Register
        </button>
      </form>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
